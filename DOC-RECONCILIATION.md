# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which
treats the code as read-only ground truth and edits only documentation so it reflects the
repo's actual current state. No code was changed and no builds or tests were run. Its purpose
is to provide an auditable record of what the reconciliation pass found and changed so any
reviewer can verify the work at a glance.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `Cartograph/App/CartographApp.swift:1-10`, `Cartograph/Views/ContentView.swift:1-239`
The README and CLAUDE.md introductions accurately describe a macOS procedural world-map generator
with a portolan-chart renderer. Entry point is `@main struct CartographApp: App` with a
`WindowGroup { ContentView() }` scene. No changes made.

---

### 2. Current state

**Status:** `consistent`
**Evidence:** all source files under `Cartograph/Pipeline/`, `Cartograph/Rendering/`,
`Cartograph/Views/`, `Cartograph/Model/`

Phase 4 complete is confirmed by code:
- Phase 0: `CartographApp.swift`, `Cartograph/Shaders/Triangle.metal` — scaffold and Metal smoke test present.
- Phase 1+2: `TectonicSimulator.swift`, `ErosionEngine.swift`, `RiverNetworkGenerator.swift`, `ClimateModel.swift` — full pipeline present and wired in `TerrainEngine.runFullPipeline()`.
- Phase 3: All 7 render passes present — `ParchmentPass`, `TerrainPass`, `CoastlinePass`, `RiverPass`, `MountainPass`, `LabelPass`, `DecorPass` — including compass rose and sea-monster rendering in `DecorPass.swift`.
- Phase 4: `SettlementPlacer.swift`, `SettlementOverlayView.swift`, `CartographDocument.swift` (static save/load), save/open/export toolbar buttons in `ContentView.swift`.

11 test files present in `CartographTests/` (more than planned — no regression).

---

### 3. Stack

**Status:** `drifted` (two items fixed)

**3a. Xcode version — `README.md` (drifted → fixed)**
- `README.md:23` — before: `macOS 14.0+, Xcode 15+`
- after: `macOS 14.0+, Xcode 26.3+`
- Evidence: `project.yml:6` sets `xcodeVersion: "26.3"`; `docs/PROOF-LOOP.md:17` independently states "Xcode 26.3+ (matches `project.yml`)".

**3b. Xcode version — `CLAUDE.md` (drifted → fixed)**
- `CLAUDE.md` (Tech Stack line): `(Xcode 15.4+)` → `(Xcode 26.3+)` (two occurrences — main body and portfolio-context duplicate)
- `CLAUDE.md` (Build & Run lines): `Xcode 15.4+` → `Xcode 26.3+` (two occurrences — main body and portfolio-context duplicate)
- Evidence: same as 3a.

**3c. Shader directory path — `CLAUDE.md` (drifted → fixed)**
- `CLAUDE.md` (Architecture section): `` `Cartograph/Metal/` `` → `` `Cartograph/Shaders/` ``
- Evidence: glob of `Cartograph/**/*.metal` confirms all seven shader files are under `Cartograph/Shaders/`; no `Metal/` subdirectory exists anywhere in the repo.

**Remaining stack claims:**
- "MetalPerformanceShaders" — `project.yml:35` links it. `consistent`.
- "No external Swift packages" — no `Package.swift` exists. `consistent`.
- "XcodeGen (project.yml)" — `project.yml` present at repo root. `consistent`.
- "NavigationSplitView" — `ContentView.swift:11`. `consistent`.
- "CoreText / Accelerate / simd" — `project.yml:36-37`, usages in `LabelPass.swift` and `TerrainEngine.swift`. `consistent`.

---

### 4. How to run

**Status:** `consistent` (after Xcode version fix above)
**Evidence:** `project.yml` exists; `Cartograph.xcodeproj` is checked in; `xcodebuild -project Cartograph.xcodeproj -scheme Cartograph` is the correct invocation and matches the scheme declared in `project.yml:18`.

---

### 5. Known risks / known issues

**Status:** `consistent`
**Evidence:** Two issues listed in both README Known Issues and CLAUDE.md Known Issues sections are substantiated by code: GPU Frame Capture advisory is warranted given the Metal render pipeline; 4096×4096 export upsampled from 1024×1024 internal is confirmed in `ExportEngine.swift:22-28` (`size: Int = 4096` on a 1024-wide source).

---

### 6. Next move

**Status:** `unverifiable`
**Reason:** Both docs state "all planned functionality shipped." The forward-looking recommendation in CLAUDE.md portfolio-context ("promote the repo beyond minimum-viable by capturing a dedicated handoff, roadmap, or discovery artifact") cannot be verified from code. Left unchanged.

---

## Contradictions for Manual Review

These drift items are in files outside the editable set (`README.md`, `CLAUDE.md`,
`AGENTS.md`, `DOC-RECONCILIATION.md`, `docs/`). A human should apply the corrections.

### A. `Makefile` — wrong build tool

The Makefile defines `build`, `test`, and `run` targets using `swift build`, `swift test`,
and `swift run`, but the repo has no `Package.swift` — it is an Xcode project, not a
SwiftPM package. These commands will fail with "no targets found" or similar. The correct
build command is `xcodebuild` (documented in README and CLAUDE.md).

**One-line fix:** Replace Makefile targets with `xcodegen generate && xcodebuild …`
equivalents, or remove the Makefile and point contributors to the README build instructions.

### B. `IMPLEMENTATION-ROADMAP.md:51-52` — non-existent source files in file tree

The file structure diagram lists two files that do not exist in the repo:
- `App/AppDelegate.swift` — the app uses a pure `@main struct CartographApp: App` (no AppDelegate).
- `Views/MapCanvasView.swift` and `Views/ExportPanel.swift` — settlement interaction and PNG export are handled directly in `ContentView.swift`; neither file exists.

**One-line fix:** Remove the three lines referencing these files from the file tree.

### C. `IMPLEMENTATION-ROADMAP.md:107-114` — test file list is incomplete

The roadmap lists only 4 test files in `CartographTests/`. The repo now has 11:
`CartographDocumentTests.swift`, `ClimateModelTests.swift`, `MarchingSquaresTests.swift`,
`SettlementPlacerTests.swift`, `ShaderTypesLayoutTests.swift`, `StrokeGeometryTests.swift`,
and `VoronoiDiagramTests.swift` were added but never reflected in the roadmap list.

**One-line fix:** Append the 7 additional filenames to the `CartographTests/` listing.

---

## Footer

**Timestamp:** 2026-05-30 20:19:45 PDT
**Branch:** `docs/truth-up-2026-05-30`
**HEAD sha reconciled against:** `2231fbdedf54114e9314b89bb3f782c0730cdfd9`
